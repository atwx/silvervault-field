{
  "name": "silvervault-field",
  "version": "1.0.0",
  "description": "SilverStripe field for selecting media from a Silvervault media database",
  "scripts": {
    "build": "NODE_ENV=production webpack --mode production --bail --progress",
    "dev": "NODE_ENV=development webpack --progress",
    "watch": "NODE_ENV=development webpack --watch --progress"
  },
  "devDependencies": {
    "@silverstripe/webpack-config": "^3",
    "webpack": "^5.74.0",
    "webpack-cli": "^5.0.0"
  },
  "dependencies": {
    "prop-types": "^15.8.1"
  }
}
